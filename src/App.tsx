import { HomePage } from './pages';

function App() {  
  return (
    <div className="App">
      {/* {isLoading ? null : <CardList results={data.results}/>} */}
      <HomePage />
    </div>
  );
}

export default App;
